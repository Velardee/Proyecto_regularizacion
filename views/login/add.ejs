<% include _header %>
    <%if (alert!=0) {
        alert.forEach(function(alert){%>
        <div id="Alert <%= alert.id %>" class="alert <%= color %>">
            <%= alert.mensaje %>
                <a id="close <%= alert.id %>" href="#" class="close" data-dismiss="alert">&times;</a>
        </div>
        <%});}%>
            <div class="row container">
                <div class="col-md-12">
                    <form action="new" method="post">
                        <div class="form-group">
                            <label for="">Nombre de usuario</label>
                            <input type="text" class="form-control" name="user">
                        </div>
                        <div class="form-group">
                            <label for="">Contraseña</label>
                            <input type="password" class="form-control" name="pass">
                        </div>
                        <div class="form-group">
                            <label for="">Correo eectronico</label>
                            <input type="email" class="form-control" name="email">
                        </div>
                        <button class="btn btn-primary">Registrarse<i class="fas fa-plus text-light"></i></button>
                    </form>
                </div>
            </div>

            <script src="/jquery/jquery.min.js"></script>
            <script src="/js/bootstrap.min.js"></script>

            <%if (alert!=0) {
                alert.forEach(function(alert){%>
                <script type="text/javascript">
                    $('#close <%= alert.id %>').click(function() {
                        $('#Alert <%= alert.id %>').hide('fade');
                    })
                </script>
                <%});}%>

                    <% include _footer %>